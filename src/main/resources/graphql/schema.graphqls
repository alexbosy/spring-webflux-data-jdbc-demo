type  User {
    id: ID!
    login: String!
    name: String!
    surname: String!
    email: String!
    type: String!
}

type Query {
    allUsers(offset: Int!, limit: Int!): [User]
    userById(id: Int!): User
}